@model IEnumerable<midterm.Models.Teacher>

@{
    ViewData["Title"] = "Index";
}

<h2>Index</h2>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.teach_name)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.teach_phone)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.teach_mail)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.teach_name)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.teach_phone)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.teach_mail)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.TeacherID">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.TeacherID">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.TeacherID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
