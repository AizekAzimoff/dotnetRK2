@model IEnumerable<midterm.Models.Ability>

@{
    ViewData["Title"] = "Index";
}

<h2>Index</h2>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.course)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.teacher)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.course.CourseID)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.teacher.TeacherID)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.AbilityID">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.AbilityID">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.AbilityID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
