@model IEnumerable<midterm.Models.Group>

@{
    ViewData["Title"] = "Index";
}

<h2>Index</h2>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.group_name)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.group_level)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.group_stud_num)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.group_name)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.group_level)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.group_stud_num)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.GroupID">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.GroupID">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.GroupID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
